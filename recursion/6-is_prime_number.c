#include "main.h"
/**
 * is_prime_number - fucntion
 *
 * @n: variable
 *
 * Return: return (1) or (0)
 */
int is_prime_number(int n)
{
	if (n % 1 && n % n)
	{
		return (1);
	}

	else if
	{
		return (0);
	}
}
